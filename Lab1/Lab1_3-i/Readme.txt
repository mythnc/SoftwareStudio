step:
1. 使用者輸入整數
沒防呆，輸入非整數程式會當掉。

2. 輸出巴斯卡三角形

利用兩個array存取第n - 1層與第n層，
利用巴斯卡三角形兩兩相加等於下一層的特型，
從0層開始，逐一往下加。
每當計算出第n層的值之後，就印出該層的element。
之後把就把此array copy給存取n - 1的array。
反覆直到列印出最後一層。

又因為巴斯卡三角形為左右對稱，
所以每次計算只需到中間值為止，
輸出時，先從index 0印到index mid，
再從index mid印到index 0。

遇到的困難：
無

