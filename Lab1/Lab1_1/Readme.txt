step:
1. 使用者輸入字串
無限制，輸入enter表示輸入完成，enter不列入處理。

2. 使用者輸入欲查詢的字元(c)
只能輸入1個字元，不可以不輸入，可以是空白字元。

3. 計算使用者輸入字元所出現的次數(sum)
使用for loop從字串的index 0到index len-1為止（len為字串長度）
逐一比對該字元與輸入的字元是否相等。
相等時，sum++。

4. 輸出使用者欲查詢的字元與次數
即輸出c與sum。

遇到的困難：
1. 不知道如何在java中寫副函式。

解決方法：
1. 全部丟到main裡面。
（可讀性大大降低）
